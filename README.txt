-- to run the application -- 
API:
1. In the command prompt, change the directory to the api folder
2. run python detection.py

Mobile Application:
1. Open mobile emulator and run the flutter program

*since this is a prototype, this application only supports one way transmission from the mobile app and the API. The API only sends the scanned result to the mobile application but the mobile application cannot send pictures to the API
